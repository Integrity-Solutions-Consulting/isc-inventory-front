version: '3.8'

services:
  angular_frontend:
    image: inventory_front:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    restart: always
    container_name: inventory_front
