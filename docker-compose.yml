version: '3.8'

services:
  angular_frontend:
    image: inventory_front:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
    restart: always
    container_name: inventory_front
